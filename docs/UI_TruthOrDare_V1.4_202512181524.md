# 真心话 / 大冒险 UI 设计

版本: V1.4_202512181524  
更新时间(UTC+8): 2025-12-18 15:24:00 CST

## 版本记录
| 版本 | 日期 | 更新内容 |
|------|------|---------|
| V1.4 | 2025-12-18 | **技术优化**：后端架构改进，不影响UI设计；保持与V1.3一致 |
| V1.3 | 2025-12-17 | 每次生成1题；禁用复制/再来一题按钮；新增"大尺度"风格（红色高亮+警告） |
| V1.2 | 2025-12-17 | 同步架构更新（EdgeOne Pages Functions） |
| V1.1 | 2025-12-17 | 代码与文档对齐；标注实现状态 |
| V1.0 | 2025-12-17 | 初始版本 |

## 说明

V1.4 版本主要为后端架构优化（统一LLM响应解析逻辑），**UI设计保持与 V1.3 完全一致**，无需前端改动。

## 页面结构（Web）✅ 已实现
- Header：标题与简要说明 ✅
- Mode 区域：按钮 `真心话` / `大冒险` ✅
- Style 区域：风格 Chips（可滚动），支持单选 ✅（**12 种风格，含大尺度**）
  - 「大尺度」使用红色高亮，选中时显示18+警告
- 参数区：语言、数量、受众年龄、尺度（使用默认值，暂未暴露 UI）
- 生成区：主按钮 `生成`，生成中禁用（防抖）✅
  - ~~再来一题按钮~~（已禁用）
- 结果区：**单条卡片**（每次只生成1题）✅
  - ~~复制按钮~~（已禁用）
- 底部：免责声明与安全提示 ✅

## 页面结构（小程序）✅ 已实现
- 顶部标题栏（原生）✅
- Mode 按钮与风格选择（原生组件）✅
- 生成按钮（大按钮），加载中禁用 ✅
- 结果展示（支持点击复制）✅；不提供分享卡片

## 交互与状态
- 必填：至少选择模式与风格后才可点击生成
- Loading：骨架屏展示；按钮禁用
- Error：顶部或 Toast 展示错误文案；`重试`按钮
- 空态：未生成时展示引导文案

## 视觉规范（初稿）
- 主色：`#7C5CFF`（活力紫）；辅色：`#FF7C5C`（活力橙）
- 字体：系统字体栈；字号：标题 20、正文 16、说明 12
- 卡片：圆角 12；阴影轻；间距 8/16
- 整体风格：派对风格，明亮活力，可加入轻微动效强化节庆氛围

## 组件清单（Web）
- `ModeSelector`、`StyleSelector`、`ParamsForm`、`GenerateButton`、`ResultList`、`ResultItem`
- `Toast`/`Modal`（错误与提示）
- **当前实现**：所有组件集成在 `App.jsx` 中（未拆分独立组件文件，V2 优化项）

## 文案与提示（示例）
- 引导：选择模式和风格，点击生成，开始互动！
- 安全：我们会过滤不适宜内容，如有问题请反馈。

## 可访问性
- 键盘焦点与可见焦点样式
- 高对比度模式兼容
- Screen Reader 文案友好
